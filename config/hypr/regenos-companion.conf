# Hyprland 0.53+ rule syntax. Source this file from your hyprland.conf.
windowrule {
    name = regenos-companion
    match:class = ^(regenos-companion)$
    float = on
    pin = on
    no_initial_focus = on
    decorate = off
    border_size = 0
    no_blur = on
    no_shadow = on
}
